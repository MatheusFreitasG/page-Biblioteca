import { Routes } from '@angular/router';
import { SelectCourseComponent } from './select-course/select-course.component';

export const routes: Routes = [
    {
        path: 'curso', component: SelectCourseComponent
    }
];
